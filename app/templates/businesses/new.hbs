
<h1 class="page-header">Register a new business</h1>
<form class="main-form">
  <div class="form-horizontal">
    <div class="form-group">
      <label for="name" class="col-sm-3 control-label">Business Name</label>
      <div class="col-sm-9">{{input type="text" id="name" class="form-control" value=name placeholder="E.g. Bob's Burgers"}}</div>
    </div>
    <div class="form-group">
      <label for="location" class="col-sm-3 control-label">Address</label>
      <div class="col-sm-9">{{input type="text" id="address" class="form-control" value=address placeholder="E.g. 335 Pioneer Way, Mountain View, CA"}}</div>
    </div>
  </div>
  <div class="form">
    <button {{action 'save'}} id="submit" class="btn btn-default">Register</button>
  </div>
  {{#if run}}
    {{#if succeeded}}
      <div data-alert class="alert alert-successful">
        The business has been registered!
      </div>
    {{else}}
      <div data-alert class="alert alert-danger">
        Failed to register the business.
      </div>
    {{/if}}
  {{/if}}
</form>
